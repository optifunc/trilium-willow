name: Build
run-name: Build ${{ github.ref_name }} · ${{ github.run_number }}.${{ github.run_attempt }}
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  build:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          ref: ${{ github.sha }}
          persist-credentials: false
      - name: Choose unique development version
        run: python3 scripts/ci-version.py build
      - uses: ./.github/actions/build-distribution
        with:
          widget-token: ${{ secrets.MR_READ_TOKEN }}
      - name: Upload distribution
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: trilium-willow-${{ env.WILLOW_VERSION }}
          if-no-files-found: error
          retention-days: 30
          path: |
            dist/trilium-willow-*.zip
            dist/willow-editor.jsx
            dist/installation.html
            dist/manifest.json
